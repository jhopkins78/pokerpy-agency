# src/goal_tracker.py

def get_active_goals(user_id: str) -> list:
    # Placeholder goal retrieval
    return [
        {"goal": "Improve post-flop decisions"},
        {"goal": "Reduce tilt frequency"}
    ]
